```bash
/* answer 3 questions:
   * 1. tagname (E): input
   * 2. attributes(name-> A, value of attribute-> t ) 
   *      name=username, id=username, type=text
   * 3. text?
   * n/a
   * 
   * Css selector: E[A=t] or [A=t] if A=id => E#t or #t, if A=class => E.t or .t
   * * //E[@A='t'] or //*[@A='t'] 
   * //E[contains(@A,t)] or //*[contains(@A,t)]
*/
```
